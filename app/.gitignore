.env
node_modules/
*.log
# Gmail OAuth token (contains sensitive credentials)
.gmail-token.json
integrations/gmail/.gmail-token.json

# Test files (dev only, not for production)
test-*.html
*-demo.html
